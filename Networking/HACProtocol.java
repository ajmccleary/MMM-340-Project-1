package Networking;

import java.net.*;

public class HACProtocol {
    String version;
    DatagramPacket packet;

    public HACProtocol (String data, int length, InetAddress address, int port, String version) {

    }
}
